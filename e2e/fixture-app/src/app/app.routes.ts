import { Routes } from '@angular/router';
import { RichTextViewDemoComponent } from './test-pages/rich-text-view-demo.component';

export const routes: Routes = [
  { path: '', component: RichTextViewDemoComponent },
];
