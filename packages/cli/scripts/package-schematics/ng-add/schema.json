{
  "$schema": "http://json-schema.org/schema",
  "$id": "shadcn-angular-package-ng-add",
  "title": "shadcn-angular package ng-add",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to configure. Defaults to every application in the workspace.",
      "$default": { "$source": "projectName" }
    }
  }
}
