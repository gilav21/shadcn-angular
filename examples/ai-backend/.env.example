# Copy to `.env` (or just `export` these) before `npm start`.
# Get a key at https://console.anthropic.com/  →  Settings → API Keys
ANTHROPIC_API_KEY=sk-ant-...

# Optional — defaults to 8787
# PORT=8787
